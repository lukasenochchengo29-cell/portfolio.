import './Hero.css'

function Hero() {
  return (
    <section id="hero" className="hero">
      {/* Hero / Landing section */}
      {/* Add: your name, title, tagline, call-to-action buttons, maybe a background animation */}
    </section>
  )
}

export default Hero
