import './Education.css'
// import { education } from '../../data/education'

function Education() {
  return (
    <section id="education" className="education">
      {/* Education section */}
      {/* Add: timeline or cards showing degrees, schools, dates */}
    </section>
  )
}

export default Education
