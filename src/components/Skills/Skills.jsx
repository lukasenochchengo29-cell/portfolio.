import './Skills.css'
// import { skills } from '../../data/skills'

function Skills() {
  return (
    <section id="skills" className="skills">
      {/* Skills / Technologies section */}
      {/* Add: skill cards or icons, progress bars, categories (frontend, backend, tools) */}
      {/* Tip: map over the skills data to render each skill dynamically */}
    </section>
  )
}

export default Skills
