import './Projects.css'
// import { projects } from '../../data/projects'

function Projects() {
  return (
    <section id="projects" className="projects">
      {/* Projects section */}
      {/* Add: project cards with image, title, description, tech stack, links */}
      {/* Tip: map over the projects data to render each project dynamically */}
    </section>
  )
}

export default Projects
